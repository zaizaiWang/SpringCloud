server:
  port: 8084
spring:
  application:
    name: spring-cloud-client-ribbon
eureka:
  instance:
    hostname: localhost
  client:
    serviceUrl:
      defaultZone: http://${eureka.instance.hostname}:8081/eureka/